---
name: Claude Code Review (reusable)
on:
  workflow_call:
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN:
        required: true
permissions: {}
jobs:
  claude-review:
    # Skip Claude review for bot PRs and fork PRs
    if: |
      !contains(github.event.pull_request.user.login, '[bot]') &&
      github.event.pull_request.head.repo.fork == false
    runs-on: ubuntu-latest
    permissions:
      contents: read  # Security: read-only sufficient for reviews
      pull-requests: write
      issues: write  # Required for gh pr comment (PRs are issues)
      id-token: write  # Required for claude-code-action OIDC auth
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          fetch-depth: 1
          persist-credentials: false  # Security: don't persist credentials
      - name: Minimize outdated Claude comments
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Fetch all claude[bot] comments matching summary or tracking patterns
          node_ids=$(gh api --paginate \
            "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments" \
            --jq '.[] | select(.user.login == "claude[bot]" and (.body | test("Claude Code Review Summary|No issues found|No new issues found|Claude finished"))) | .node_id')

          for node_id in $node_ids; do
            echo "Minimizing comment $node_id"
            gh api graphql -f query="
              mutation {
                minimizeComment(input: {subjectId: \"$node_id\", classifier: OUTDATED}) {
                  minimizedComment { isMinimized }
                }
              }"
          done
      - name: Run Claude Code Review
        id: claude-review
        uses: anthropics/claude-code-action@5d0cc745cd0cce4c0e9e0b3511de26c3bc285eb5  # v1.0.71
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          track_progress: true
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}
            COMMIT_SHA: ${{ github.event.pull_request.head.sha }}

            The PR branch is already checked out. Use Read/Grep/Glob to
            search the codebase for context.

            1. Read CLAUDE.md and any files it references to learn project
            conventions and review standards.

            2. Get the PR diff with `gh pr diff`. Fetch previous review comments
            via REST API (`gh api --paginate .../pulls/NUMBER/comments`) to get
            each comment's `id`, `node_id`, `user.login`, path, line, body.
            Also fetch review threads via GraphQL (query
            repository.pullRequest.reviewThreads with id, isResolved, and first
            comment's author login, body, path, line). Do NOT re-report
            already-flagged issues.

            3. MANDATORY — Process ALL previous claude[bot] review threads.
            Do NOT skip this step even if you find no new issues. For each
            unresolved thread, read the CURRENT file content (use Read tool,
            not the diff) and classify:
            - FIXED: resolve and minimize:
              `gh api graphql -f query='mutation { resolveReviewThread(input: {threadId: "THREAD_ID"}) { thread { isResolved } } }'`
              `gh api graphql -f query='mutation { minimizeComment(input: {subjectId: "COMMENT_NODE_ID", classifier: OUTDATED}) { minimizedComment { isMinimized } } }'`
            - STILL PRESENT: leave open, count in summary
            - ALREADY RESOLVED: skip
            Note: use thread `id` for resolveReviewThread, comment `node_id`
            for minimizeComment. You cannot score 5/5 confidence if unresolved
            issues remain.

            4. MANDATORY — Triage Copilot review comments.
            Check Copilot status:
            `gh api repos/${{ github.repository }}/commits/COMMIT_SHA/check-runs --jq '.check_runs[] | select(.name == "Copilot code review") | {status, conclusion}'`
            If empty output, Copilot is not enabled — still triage any existing
            Copilot threads from previous commits. If in_progress, poll every
            30s (max 5 min). Then find unresolved Copilot threads: filter
            GraphQL reviewThreads where isResolved == false and first comment
            author is "copilot-pull-request-reviewer". Cross-reference with
            REST comments (from step 2) to get COMMENT_ID and node_id for
            mutations. For each, read the CURRENT file content and classify:
            - FIXED: reply, resolve, and minimize:
              `gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/comments/COMMENT_ID/replies --method POST -f body="Fixed — this issue has been addressed in the current code."`
              `gh api graphql -f query='mutation { resolveReviewThread(input: {threadId: "THREAD_ID"}) { thread { isResolved } } }'`
              `gh api graphql -f query='mutation { minimizeComment(input: {subjectId: "COMMENT_NODE_ID", classifier: OUTDATED}) { minimizedComment { isMinimized } } }'`
            - VALID: leave open, count in summary
            - FALSE POSITIVE: reply with reason, resolve thread:
              `gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/comments/COMMENT_ID/replies --method POST -f body="Dismissed — <reason>."`
              `gh api graphql -f query='mutation { resolveReviewThread(input: {threadId: "THREAD_ID"}) { thread { isResolved } } }'`
            Dismiss cosmetic nitpicks — only acknowledge real bugs/security issues.

            5. Post inline comments using `mcp__github_inline_comment__create_inline_comment`
            with `confirmed: true`. Only comment on lines in the diff (NEW version).
            Use `startLine` + `line` for multi-line ranges. Format each comment:
            severity emoji + one-sentence summary + fix suggestion, then a
            collapsible `<details>` block with extended reasoning.
            Emojis: 🔴 bug, 🟠 security, 🟡 risky pattern, 🔵 CLAUDE.md violation.

            6. Post a PR summary via `gh pr comment` with this structure:
            - `## Claude Code Review Summary` (or `## ✅ No issues found` /
              `## ℹ️ No new issues found — N Copilot finding(s) acknowledged`)
            - `<details><summary><h3>Confidence Score: X/5</h3></summary>`
              with brief assessment (5/5 = simple changes fully understood,
              3/5 = complex changes needing human judgment)
            - `<details><summary><h3>Copilot Review Triage</h3></summary>`
              with triage counts and per-comment actions (OMIT if no Copilot threads)
            - `<details><summary><h3>Important Files Changed</h3></summary>`
              with markdown table (filename | overview)

            RULES:
            - Confidence >= 80 to report an issue. False positives waste reviewer time.
            - For CLAUDE.md violations: verify the rule exists and quote it.
            - Focus: bugs, security, breaking changes, CLAUDE.md violations, missing
              error handling, missing tests, critical performance issues.
            - Ignore: unchanged code, style/formatting, optional improvements,
              dependency versions. Do NOT run build tools or provide praise.
            - Every API call is permanent. Do NOT post test or placeholder comments.
            - Only post GitHub comments — don't submit review text as messages.
          settings: |
            {
              "permissions": {
                "allow": [
                  "Read", "Grep", "Glob", "BatchTool",
                  "mcp__github_inline_comment__create_inline_comment",
                  "Bash(gh pr comment:*)",
                  "Bash(gh pr diff:*)",
                  "Bash(gh pr view:*)",
                  "Bash(gh api:*)"
                ]
              }
            }
          claude_args: --max-turns 30
