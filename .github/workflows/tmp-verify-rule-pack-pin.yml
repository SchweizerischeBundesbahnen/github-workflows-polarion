---
name: TEMP verify rule-pack pin
on:
  push:
    branches: [fix/semgrep-rule-pack-pin]
permissions: {}
jobs:
  semgrep:
    uses: ./.github/workflows/reusable-polarion-semgrep.yml
    permissions:
      contents: read
      security-events: write
      actions: read
