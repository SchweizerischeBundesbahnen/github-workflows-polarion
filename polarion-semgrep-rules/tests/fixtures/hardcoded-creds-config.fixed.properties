# polarion.properties (fixed)
# ok: polarion-hardcoded-creds-config — env var placeholder
db.password=${env.DB_PASSWORD}
# ok: polarion-hardcoded-creds-config — Polarion property placeholder
ldap.password=${ldap.password.encrypted}
# ok: polarion-hardcoded-creds-config — Maven filter placeholder
admin.password=@ADMIN_PASSWORD@
# ok: polarion-hardcoded-creds-config — empty (intentionally not set)
api.key=
# ok: polarion-hardcoded-creds-config — system property reference
mail.password=${sys.mail.password}
# ok: polarion-hardcoded-creds-config — different (non-cred) key
db.user=polarion
