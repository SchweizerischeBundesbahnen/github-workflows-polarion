---
rules:
  # Secrets are protected by GitHub's fork PR approval requirement,
  # which prevents untrusted workflows from accessing secrets without explicit approval.
  secrets-outside-env:
    ignore: [claude-code-review.yml, reusable-claude-code-review.yml, claude-code-review-triage.yml, reusable-claude-code-review-triage.yml]
